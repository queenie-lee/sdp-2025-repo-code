package helloworld;
/**
 * Problems:
 * 1. Delivers only one message (no arguments)
 * 2. Relies on system.args = does not work with GUI
 * */
public class HelloWorld {
    public static void main(String... args) {
        System.out.println("Hello World!");
    }
}